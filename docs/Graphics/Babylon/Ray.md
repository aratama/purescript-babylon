## Module Graphics.Babylon.Ray

#### `createRay`

``` purescript
createRay :: forall eff. Vector3 -> Vector3 -> Eff (babylon :: BABYLON | eff) Ray
```

#### `createRayWithLength`

``` purescript
createRayWithLength :: forall eff. Vector3 -> Vector3 -> Number -> Eff (babylon :: BABYLON | eff) Ray
```


